mutation AddSession($input: AddSessionInputType!) {
  addSession(input: $input) {
    errors {
      field
      message
    }
    session {
      id
      name
      startTime
      endTime
    }
  }
}
