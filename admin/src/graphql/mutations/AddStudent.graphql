mutation AddStudent($input: AddStudentInputType!) {
  addStudent(input: $input) {
    errors {
      field
      message
    }
    student {
      id
      username
      email
      registrationNumber
      oneTimePassword
      gender
      address
      contactNumber
      createdAt
      updatedAt
    }
  }
}
