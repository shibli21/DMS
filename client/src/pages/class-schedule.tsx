import { Container, Heading } from "@chakra-ui/react";
import React from "react";

interface Props {}

const ClassSchedule = (props: Props) => {
  return <Heading>Class Schedule</Heading>;
};

export default ClassSchedule;
